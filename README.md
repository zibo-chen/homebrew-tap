# Homebrew tap for Newbee OCR CLI

Install `nbocr` on macOS or Linux:

```bash
brew install zibo-chen/tap/nbocr
```

Formula updates are published automatically from
[`zibo-chen/newbee-ocr-cli`](https://github.com/zibo-chen/newbee-ocr-cli).
